schema {
  query: Query
  mutation: Mutation
}

type Query {
  findUserById(id: ID!): User
  findUserByEmail(email: String!): User
}

type Mutation {
  removeUserById(id: ID!): Boolean
}

type User {
  id: ID
  firstName: String!
  lastName: String!
  emailAddress: String!
  payment: PaymentInfo
  createdAt: String!
  updatedAt: String
}

type PaymentInfo {
  providerId: String!
  providerType: String!
}

schema {
  query: Query
  mutation: Mutation
}

type Query {
  findUserById(id: ID!): User # admin priviledge
  findUserByEmail(email: String!): User
}

type Mutation {
  removeUserById(id: ID!): Boolean
}

type User {
  uid: ID
  firstName: String!
  lastName: String!
  emailAddress: String!
  payment: PaymentInfo
  createdAt: String!
  updatedAt: String
}

type PaymentInfo {
  providerId: String!
  providerType: String!
}

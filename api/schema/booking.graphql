schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  makeBooking(
    amount: Int!
    currency: String!
    userId: ID!
    listingId: ID!
    items: [StorageItem]
  ): Boolean
}

input StorageItem {
  name: String
  imageUrls: String
}

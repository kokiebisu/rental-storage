## Query
- type: AWS_LAMBDA
  name: FindUserByIdFunction
  config:
    lambdaFunctionArn: arn:aws:lambda:${self:provider.region}:${aws:accountId}:function:${self:custom.namespace}-user-${self:provider.stage}-appsync-FindUserById

- type: AWS_LAMBDA
  name: FindUserByEmailFunction
  config:
    lambdaFunctionArn: arn:aws:lambda:${self:provider.region}:${aws:accountId}:function:${self:custom.namespace}-user-${self:provider.stage}-appsync-FindUserByEmail

## Mutation
- type: AWS_LAMBDA
  name: RemoveUserByIdFunction
  config:
    lambdaFunctionArn: arn:aws:lambda:${self:provider.region}:${aws:accountId}:function:${self:custom.namespace}-user-${self:provider.stage}-appsync-RemoveUserById

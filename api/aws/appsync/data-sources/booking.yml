## Mutation
- type: AWS_LAMBDA
  name: MakeBookingFunction
  config:
    lambdaFunctionArn: arn:aws:lambda:${self:provider.region}:${aws:accountId}:function:${self:custom.namespace}-booking-${self:provider.stage}-appsync-MakeBooking

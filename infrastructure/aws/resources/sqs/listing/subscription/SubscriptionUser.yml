Type: "AWS::SNS::Subscription"
DependsOn:
  - QueueListing
  - TopicUser
Properties:
  Protocol: sqs
  TopicArn:
    Fn::Join:
      - ":"
      - - "arn:aws:sns"
        - ${aws:region}
        - ${aws:accountId}
        - ${self:provider.stage}-${self:custom.namespace}-user-topic
  Endpoint:
    Fn::Join:
      - ":"
      - - "arn:aws:sqs"
        - ${aws:region}
        - ${aws:accountId}
        - ${self:provider.stage}-${self:custom.namespace}-listing-queue

Type: AWS::S3::Bucket
DeletionPolicy: Delete
Properties:
  BucketName: ${self:provider.stage}-${aws:accountId}-rental-a-locker-listing-profile
  AccessControl: PublicReadWrite
  CorsConfiguration:
    CorsRules:
      - AllowedMethods:
          - GET
          - PUT
          - POST
          - HEAD
        AllowedOrigins:
          - "*"
        AllowedHeaders:
          - "*"

schema {
  query: Query
}

type Query {
  space(id: ID!): Space
  spaces(userId: ID): [Space]
}

type Space {
  id: String
  title: String
  lenderId: String
  location: Location
  imageUrls: [String]
  description: String
  createdAt: String
  updatedAt: String
}

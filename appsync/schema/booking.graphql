schema {
  query: Query
  mutation: Mutation
}

type Query {
  booking(id: ID!): Booking
}

type Mutation {
  bookingCreate(listingId: ID!, items: [ItemInput]): UId
}

type Booking {
  id: ID
  status: String
  userId: String
  listingId: String
  items: [Item]
  createdAt: String
  updatedAt: String
}

input ItemInput {
  name: String
  imageUrls: [String]
}

type Item {
  name: String
  imageUrls: [String]
}

schema {
  query: Query
  mutation: Mutation
}

type Query {
  booking(id: ID!): Booking
  bookings(spaceId: ID!): [Booking]
}

type Mutation {
  bookingCreate(spaceId: ID!, items: [ItemInput]): UId
}

input ItemInput {
  name: String
  imageUrls: [String]
}

schema {
  query: Query
  mutation: Mutation
}

type Mutation {
  makeBooking(
    amount: Int!
    currency: String!
    userId: ID!
    listingId: ID!
    items: [Item]
  ): Boolean
}

input Item {
  name: String
  imageUrls: [String]
}

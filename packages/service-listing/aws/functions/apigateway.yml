ListingServiceController:
  handler: dist/adapter/api/controller/index.handler
  name: ${self:provider.stage}-${self:custom.service}-rest-controller
  events:
    - http: ANY /listings
    - http: ANY /listings/{proxy+}

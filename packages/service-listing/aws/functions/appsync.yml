# Query
AppSyncFindListingById:
  handler: dist/adapter/graphql/query/findListingById.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-findListingById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${env:DB_NAME}
    USERNAME: ${env:DB_USERNAME}
    PASSWORD: ${env:DB_PASSWORD}

AppSyncFindListingsWithinLatLng:
  handler: dist/adapter/graphql/query/findListingsWithinLatLng.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-findListingsWithinLatLng
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${env:DB_NAME}
    USERNAME: ${env:DB_USERNAME}
    PASSWORD: ${env:DB_PASSWORD}

# Mutation
AppSyncAddListing:
  handler: dist/adapter/graphql/mutation/addListing.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-addListing
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${env:DB_NAME}
    USERNAME: ${env:DB_USERNAME}
    PASSWORD: ${env:DB_PASSWORD}

AppSyncRemoveListingById:
  handler: dist/adapter/graphql/mutation/removeListingById.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-removeListingById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${env:DB_NAME}
    USERNAME: ${env:DB_USERNAME}
    PASSWORD: ${env:DB_PASSWORD}

# Query
FindListingByIdQuery:
  handler: dist/adapter/api/graphql/FindListingById.handler
  name: ${self:provider.stage}-${self:custom.service}-graphql-find-listing-by-id

FindListingsWithinLatLngQuery:
  handler: dist/adapter/api/graphql/FindListingsWithinLatLng.handler
  name: ${self:provider.stage}-${self:custom.service}-graphql-find-listings-within-lat-lng

FindMyListingsQuery:
  handler: dist/adapter/api/graphql/FindMyListings.handler
  name: ${self:provider.stage}-${self:custom.service}-graphql-find-my-listings

# Mutation
AddListingMutation:
  handler: dist/adapter/api/graphql/AddListing.handler
  name: ${self:provider.stage}-${self:custom.service}-graphql-add-listing

RemoveListingByIdMutation:
  handler: dist/adapter/api/graphql/RemoveListingById.handler
  name: ${self:provider.stage}-${self:custom.service}-graphql-remove-listing-by-id

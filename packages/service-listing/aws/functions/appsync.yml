# Query
FindListingByIdQuery:
  handler: dist/adapter/in/api/graphql/query/FindListingById.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-FindListingById
  environment:
    DB_HOST: ${self:custom.DB_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    DB_USERNAME: ${self:custom.DB_USERNAME}
    DB_PASSWORD: ${self:custom.DB_PASSWORD}
    SERVICE_API_ENDPOINT: ${self:custom.SERVICE_API_ENDPOINT}
    ACCOUNT_ID: ${aws:accountId}

FindListingsWithinLatLngQuery:
  handler: dist/adapter/in/api/graphql/query/FindListingsWithinLatLng.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-FindListingsWithinLatLng
  environment:
    DB_HOST: ${self:custom.DB_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    DB_USERNAME: ${self:custom.DB_USERNAME}
    DB_PASSWORD: ${self:custom.DB_PASSWORD}
    SERVICE_API_ENDPOINT: ${self:custom.SERVICE_API_ENDPOINT}
    ACCOUNT_ID: ${aws:accountId}

# Mutation
AddListingMutation:
  handler: dist/adapter/in/api/graphql/mutation/AddListing.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-AddListing
  environment:
    DB_HOST: ${self:custom.DB_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    DB_USERNAME: ${self:custom.DB_USERNAME}
    DB_PASSWORD: ${self:custom.DB_PASSWORD}
    SERVICE_API_ENDPOINT: ${self:custom.SERVICE_API_ENDPOINT}
    ACCOUNT_ID: ${aws:accountId}

RemoveListingByIdMutation:
  handler: dist/adapter/in/api/graphql/mutation/RemoveListingById.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-appsync-RemoveListingById
  environment:
    DB_HOST: ${self:custom.DB_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    DB_USERNAME: ${self:custom.DB_USERNAME}
    DB_PASSWORD: ${self:custom.DB_PASSWORD}
    SERVICE_API_ENDPOINT: ${self:custom.SERVICE_API_ENDPOINT}
    ACCOUNT_ID: ${aws:accountId}

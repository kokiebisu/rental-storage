service: rental-a-locker-booking
frameworkVersion: "3"

plugins:
  - serverless-deployment-bucket
  - serverless-export-env

provider:
  name: aws
  runtime: nodejs14.x
  stage: ${opt:stage, 'local'}
  region: us-east-1
  lambdaHashingVersion: 20201221
  iam:
    role: !ImportValue ${self.provider.stage}-ExportedRoleLambdaArn
  deploymentBucket:
    name: rental-a-locker-booking-resources
  apiGateway:
    restApiId: !ImportValue ${self.provider.stage}-ApiGatewayRestApiId
    restApiRootResourceId: !ImportValue ${self.provider.stage}-ApiGatewayRootResourceId

custom:
  namespace: rental-a-locker
  service: booking
  TABLE_NAME: !ImportValue ${self.provider.stage}-ExportedBookingDynamoDBTableName
  SERVICE_API_ENDPOINT:
    Fn::Join:
      - ""
      - - "https://"
        - ${self:provider.apiGateway.restApiId}
        - ".execute-api."
        - ${self:provider.region}
        - ".amazonaws.com/"
        - ${self:provider.stage}

functions:
  - ${file(./aws/functions/apigateway.yml)}
  - ${file(./aws/functions/appsync.yml)}

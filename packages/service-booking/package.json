{
  "name": "@rental-storage-project/booking",
  "private": true,
  "scripts": {
    "deploy": "export AWS_PROFILE=rental-storage && sls deploy --config serverless.yml --stage local",
    "remove": "sls remove --config serverless.yml --stage local"
  },
  "devDependencies": {
    "serverless": "3.28.1",
    "serverless-deployment-bucket": "1.6.0"
  }
}

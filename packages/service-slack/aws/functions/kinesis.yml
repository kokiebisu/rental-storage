SlackUserEventMessageConsumer:
  image:
    name: app
    command:
      - adapter/api/kinesis/user.handler
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-consumer-user
  events:
    - stream:
        type: kinesis
        arn: !ImportValue ExportedKinesisStreamArn
service: rental-storage-image
frameworkVersion: "3"

plugins:
  - serverless-deployment-bucket

provider:
  name: aws
  runtime: python3.8
  stage: dev
  region: us-east-1
  lambdaHashingVersion: 20201221
  deploymentBucket:
    name: rental-storage-image-resources

custom:
  namespace: rental-storage
  service: image

functions:
  - ${file(./aws/functions/appsync.yml)}

GenerateCustomerIdApiGateway:
  image:
    name: app
    command:
      - bin/create-payment-customer
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-rest-create_payment_customer
  events:
    - http:
        path: /payments/customer
        method: post

GenerateCustomerIdApiGateway:
  handler: bin/generate_customer_id
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-rest-generate-customer-id
  environment:
    SERVICE_API_ENDPOINT: ${self:custom.SERVICE_API_ENDPOINT}
    ACCOUNT_ID: ${aws:accountId}
  package:
    exclude:
      - ./**
    include:
      - ./bin/generate_customer_id
  events:
    - http:
        path: /payment/customerId
        method: get
# /payment/customerId?emailAddress=..&firstName=..&lastName=..

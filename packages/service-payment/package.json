{
  "name": "@rental-storage-project/payment",
  "scripts": {
    "deploy:local": "sls deploy --config serverless.yml --stage local",
    "deploy:dev": "sls deploy --config serverless.dev.yml --stage dev",
    "remove": "sls remove",
    "generate:env:local": "sls export-env --stage local",
    "generate:env:dev": "sls export-env --stage dev"
  },
  "devDependencies": {
    "serverless": "2.72.3",
    "serverless-deployment-bucket": "1.6.0",
    "serverless-export-env": "2.2.0"
  }
}

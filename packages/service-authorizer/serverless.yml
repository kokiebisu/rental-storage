service: rental-a-locker-authorizer
frameworkVersion: "3"

plugins:
  - serverless-deployment-bucket

provider:
  name: aws
  runtime: go1.x
  stage: dev
  region: us-east-1
  lambdaHashingVersion: 20201221
  iam:
    role: !ImportValue ExportedRoleLambdaArn
  deploymentBucket:
    name: rental-a-locker-authorizer-resources

custom:
  namespace: rental-a-locker
  service: authorizer

functions:
  Authorize:
    handler: bin/authorize
    name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-Authorizer
    package:
      include:
        - ./bin/authorize

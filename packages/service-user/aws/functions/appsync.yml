# Graphql
## Query
AppSyncFindGuestById:
  handler: dist/adapter/graphql/query/findGuestById.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-findGuestById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}

AppSyncFindHostById:
  handler: dist/adapter/graphql/query/findHostById.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-findHostById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}
## Mutation
AppSyncRegisterGuest:
  handler: dist/adapter/graphql/mutation/registerGuest.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-registerGuest
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}
AppSyncRegisterHost:
  handler: dist/adapter/graphql/mutation/registerHost.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-registerHost
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}
AppSyncRemoveGuestById:
  handler: dist/adapter/graphql/mutation/removeGuestById.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-removeGuestById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}
AppSyncRemoveHostById:
  handler: dist/adapter/graphql/mutation/removeHostById.handler
  name: ${self:custom.namespace}-${self:custom.service}-appsync-removeHostById
  environment:
    MYSQL_HOST: ${self:custom.AURORA_HOST}
    DB_NAME: ${self:custom.DB_NAME}
    USERNAME: ${self:custom.DB_USERNAME}
    PASSWORD: ${self:custom.DB_PASSWORD}

## Query
FindMeQuery:
  image:
    name: app
    command:
      - dist/adapter/api/graphql/FindMe.handler
  # handler: dist/adapter/api/graphql/FindMe.handler
  name: ${self:provider.stage}-app-${self:custom.service}-graphql-find-me

FindUserByIdQuery:
  image:
    name: app
    command:
      - dist/adapter/api/graphql/FindUserById.handler
  # handler: dist/adapter/api/graphql/FindUserById.handler
  name: ${self:provider.stage}-app-${self:custom.service}-graphql-find-user-by-id

FindUserByEmailQuery:
  image:
    name: app
    command:
      - dist/adapter/api/graphql/FindUserByEmail.handler
  # handler: dist/adapter/api/graphql/FindUserByEmail.handler
  name: ${self:provider.stage}-app-${self:custom.service}-graphql-find-user-by-email
## Mutation
RemoveUserByIdMutation:
  image:
    name: app
    command:
      - dist/adapter/api/graphql/RemoveUserById.handler
  # handler: dist/adapter/api/graphql/RemoveUserById.handler
  name: ${self:provider.stage}-app-${self:custom.service}-graphql-remove-user-by-id

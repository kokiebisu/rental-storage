## SQS
SQSBookingCreated:
  handler: dist/adapter/consumer/booking/bookingCreated.handler
  name: ${self:custom.namespace}-${self:custom.service}-${self:provider.stage}-sqs-bookingCreated
  events:
    - sqs:
        enabled: true
        arn:
          Fn::GetAtt:
            - SQSQueue
            - Arn

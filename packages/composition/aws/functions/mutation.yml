RemoveUserById:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.removeUserById
  name: ${self:provider.stage}-app-${self:custom.service}-remove-user-by-id

AddListing:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.addListing
  name: ${self:provider.stage}-app-${self:custom.service}-add-listing

RemoveListingById:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.removeListingById
  name: ${self:provider.stage}-app-${self:custom.service}-remove-listing-by-id

CreateBooking:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.createBooking
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-create-booking

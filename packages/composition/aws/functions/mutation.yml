RemoveUserById:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.removeUserById
  name: ${self:provider.stage}-app-${self:custom.service}-remove-user-by-id

AddListing:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.addListingMutation
  name: ${self:provider.stage}-app-${self:custom.service}-add-listing

RemoveListingById:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.removeListingById
  name: ${self:provider.stage}-app-${self:custom.service}-remove-listing-by-id

MakeBooking:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.makeBooking
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-make-booking

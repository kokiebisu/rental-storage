DeleteUser:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.deleteUser
  name: ${self:provider.stage}-app-${self:custom.service}-delete-user

CreateListing:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.createListing
  name: ${self:provider.stage}-app-${self:custom.service}-create-listing

DeleteListing:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.deleteListing
  name: ${self:provider.stage}-app-${self:custom.service}-delete-listing

CreateBooking:
  image:
    name: app
    command:
      - dist/adapter/resolver/mutation.createBooking
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-create-booking

FindMe:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.findMe
  name: ${self:provider.stage}-app-${self:custom.service}-find-me

FindUser:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.findUser
  name: ${self:provider.stage}-app-${self:custom.service}-find-user

FindListing:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.findListing
  name: ${self:provider.stage}-app-${self:custom.service}-find-listing

FindListings:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.findListings
  name: ${self:provider.stage}-app-${self:custom.service}-find-listings

FindBooking:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.findBooking
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-find-booking

GetPresignedURL:
  image:
    name: app
    command:
      - dist/adapter/resolver/query.getPresignedURL
  name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-get-presigned-url
  environment:
    ACCOUNT_ID: ${aws:accountId}
    STAGE: ${self:provider.stage}
# FindAllCreatedBookings:
#   image:
#     name: app
#     command:
#       - dist/adapter/resolver/query.findAllCreatedBookings
#   name: ${self:provider.stage}-${self:custom.namespace}-${self:custom.service}-find-all-created-bookings
